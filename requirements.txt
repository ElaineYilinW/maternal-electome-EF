# Plain-text dependency mirror for users who prefer
# `pip install -r requirements.txt` over `pip install -e .`. Single
# source of truth lives in pyproject.toml; this file just tracks it.

# Core scientific stack
numpy>=1.21
pandas>=1.4
scipy>=1.7
scikit-learn>=1.0

# Deep learning. The shipped .pt files are stored as portable
# {state_dict, config} dicts, so any torch >= 1.10 on any supported
# Python (3.8 - 3.13) can load them bit-exactly.
torch>=1.10

# Provides `torchbd` (BetaDivLoss for the NMF reconstruction loss)
# Author: Billy Carson, Duke BME (BSD-3-Clause)
beta-divergence-metrics

# Plotting
matplotlib>=3.5
seaborn>=0.11

# Utilities
tqdm>=4.62
joblib>=1.1
openpyxl>=3.0

# Jupyter (for examples/demo.ipynb and the 6 task notebooks)
jupyter
notebook

# Carlson Lab LFP utilities (GitHub-only, NOT on PyPI).
# Only needed by notebooks/00_data_preprocessing.ipynb (raw .mat -> .pkl
# feature extraction). Demo users do NOT need this.
# Skip with: pip install -r requirements.txt --no-deps lpne
git+https://github.com/carlson-lab/lpne.git
